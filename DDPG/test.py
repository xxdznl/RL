import random
import numpy as np

# mem = np.zeros((3,2+2+1+1))
# s = np.array([1,2])
# a = np.array([1])
# s_ = np.array([3,4])
# r = np.array([1])
# tran = np.hstack((s,a,s_,r))
# print(tran)
# mem[2,:] =tran
# print(mem)
# sample_index = np.random.choice(3,2,replace=False)
# print(f'choice_index+{sample_index}')
# sample_index = random.sample(range(0, 3),2)
# print(f'sample_index+{sample_index}')
# new_mem = mem[sample_index,:]
# print(new_mem)
